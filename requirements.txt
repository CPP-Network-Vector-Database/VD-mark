streamlit>=1.20.0
pandas>=1.3.0
plotly>=5.0.0
numpy>=1.21.0
sentence-transformers>=2.2.0
psycopg2>=2.9.0
weaviate-client>=3.23.0
qdrant-client>=1.3.0
pymilvus>=2.2.0
chromadb>=0.4.0

# For database modules (may require local/remote DBs running)
# faiss is often installed via conda or system package, but Python wheel is available for Linux:
faiss-cpu>=1.7.3; platform_system=="Linux"

# Optional: torch for sentence-transformers (if not installed as dependency)
torch>=1.9.0 